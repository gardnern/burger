<h1>EAT-DA-BURGER!</h1>
<main>


    <div class="row">
      <div class="col-text-center">
       
            <h3>READY to Eat Burger:</h3>
                {{#each burger}}
                   {{#unless devoured}}
                      {{> burger/delete devoured=false}}
                   {{/unless}}
                {{/each}}
           
      </div>
           <h2>Devoured Burgers:</h2>
      <div class="col s6">
        {{#each burger}}
          {{#if this.devoured}}
            <div class="burgerEaten">
             
               <h3>{{> burger/delete devoured=true}}</h3>
              
            </div>
          {{/if}}
        {{/each}}
      </div>
    </div>
  
</main>

<h2>Let's Create A New Burger!</h2>
<hr>

<h2>Name your Burger!</h2>
  <form class = "create-form">

    <div class = "form-group">
        <label form="burger">Burger Name</label>
        <input type = "text" id="burger" name="burger_name">
    </div>
  <div class = "form-group">
    <label form="devoured">Are you ready to eat this burger?</label<br>
          <input type="radio" name="devoured" value="1" checked>Yes, I'm hungry!<br>
          <input type="radio" name="devoured" value="0"> Maybe, later
  </div>
  
  <button type="submit">Make this burger</button>
  </form>
  {{!-- <div class="row">
  <div class="col s12"> --}}





    {{!-- <form action="/api/burger" method="POST">
      <input type="text" id="name" name="burger_name" placeholder="Burger Name">
      <button type="submit" class="addBurgerButton">Add the burger</button>
      <h2>Would you like to order some more?</h2>
    </form> --}}
  {{!-- </div>
</div> --}}