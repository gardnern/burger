<h1>EAT-DA-BURGER!!!</h1>

<h2>Type the kind of burger you would like to eat!</h2>

<ul>
  {{#each burger_data}}
    {{#unless this.devoured}}
      {{this.id}}
       {{this.name}}
        {{this.burger_name}}
    {{/unless}}
  {{/each}}
</ul>

{{!-- <h2>Cats that are sleepy!</h2>

<ul>
  {{#each burger_data}}
    {{#if this.devoured}}
      {{> burg sleep=false}}
    {{/if}}
  {{/each}}
</ul> --}}

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="new-burger">Burger Name:</label>
    <input type="text" id="burger_name" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">Devoured</label><br>
    <input type="radio" name="devoured" value="1" checked> YUM!<br>
    <input type="radio" name="devoured" value="0"> Not Hungry!
  </div>

  <button type="submit">Add Burger</button>
</form>
